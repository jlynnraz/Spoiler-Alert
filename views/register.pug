extends layout.pug

block content
    section#content
        #discussion
            h2 Register Account
            form
                input(
                    type="text"
                    name="username"
                    id="username"
                    label="Username"
                )
                input(
                    type="password"
                    name="password"
                    id="password"
                    label="Password"
                )
                button#joinSubmit Join
    script(src='/assets/js/register.js')